
const funcionPrin = (callback, msg) => {
    callback(msg);
}

const minus = (msg) => {
    console.log(msg.toLowerCase());
}

const mayus = (msg) => {
    console.log(msg.toUpperCase());
}

const fun = ( setTimeout(() => {

}),3000)
const fun = ( setTimeout(() => {

}),3000)